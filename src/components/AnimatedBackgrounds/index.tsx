import * as S from "./style";

export default function AnimatedBackgrounds() {
  const List = [
    1, 12, 8, 6, 4, 18, 5, 23, 11, 14, 22, 3, 27, 21, 15, 16, 2, 22, 7, 21, 10,
    20,
  ];
  return (
    <S.Section>
      {List.map((e, index) => {
        return <S.I num={e} key={index}></S.I>;
      })}
      {List.map((e, index) => {
        return <S.I num={e} key={index}></S.I>;
      })}
      {List.map((e, index) => {
        return <S.I num={e} key={index}></S.I>;
      })}
      {List.map((e, index) => {
        return <S.I num={e} key={index}></S.I>;
      })}
      {List.map((e, index) => {
        return <S.I num={e} key={index}></S.I>;
      })}
      {List.map((e, index) => {
        return <S.I num={e} key={index}></S.I>;
      })}
      {List.map((e, index) => {
        return <S.I num={e} key={index}></S.I>;
      })}
      {List.map((e, index) => {
        return <S.I num={e} key={index}></S.I>;
      })}
      {List.map((e, index) => {
        return <S.I num={e} key={index}></S.I>;
      })}
      {List.map((e, index) => {
        return <S.I num={e} key={index}></S.I>;
      })}
      {List.map((e, index) => {
        return <S.I num={e} key={index}></S.I>;
      })}
      {List.map((e, index) => {
        return <S.I num={e} key={index}></S.I>;
      })}
      {List.map((e, index) => {
        return <S.I num={e} key={index}></S.I>;
      })}
      {List.map((e, index) => {
        return <S.I num={e} key={index}></S.I>;
      })}
      {List.map((e, index) => {
        return <S.I num={e} key={index}></S.I>;
      })}
      {List.map((e, index) => {
        return <S.I num={e} key={index}></S.I>;
      })}
    </S.Section>
  );
}
