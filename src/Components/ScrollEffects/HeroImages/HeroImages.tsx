import { HeroImageBox, HeroImageLayout } from "./Style";

export default function HeroIamges() {''

    return (
        <>
            <HeroImageLayout>
                <HeroImageBox>

                </HeroImageBox>
            </HeroImageLayout>
        </>
    );
}